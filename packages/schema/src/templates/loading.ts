// This file is generated by @nuxt/ui-templates and committed to the repository.
// Do not edit it directly: edit the source in packages/ui-templates/templates/
// and run `pnpm --filter @nuxt/ui-templates build`.

import { escapeHtml } from '@vue/shared'

export type DefaultMessages = Record<"appName" | "loading" | "version", string | boolean | number >
const _messages = {"appName":"Nuxt","loading":"Loading","version":"4.0"}
export const template = (messages: Partial<DefaultMessages>): string => {
  messages = { ..._messages, ...messages }
  return "<!DOCTYPE html><html lang=\"en\"><head><title>" + escapeHtml(messages.loading) + " | " + escapeHtml(messages.appName) + "</title><meta charset=\"utf-8\"><meta content=\"width=device-width,initial-scale=1,minimum-scale=1\" name=\"viewport\"><style>:root{--nuxt-mint:#00dc82;--nuxt-mist:rgba(232,255,244,.88);--nuxt-dim:rgba(0,220,130,.55);--nuxt-accent:#00dc82;--nuxt-logo:min(15vw, 99px)}*,:after,:before{box-sizing:border-box}body,html{margin:0;width:100%;height:100%;background:#000;color:var(--nuxt-mist)}body{display:flex;align-items:center;justify-content:center;overflow:hidden;font-family:ui-sans-serif,system-ui,sans-serif;-webkit-font-smoothing:antialiased}#nuxt-shader,svg{display:block}#nuxt-shader{position:fixed;inset:0;width:100%;height:100%;z-index:0;touch-action:none;opacity:0;transition:opacity .4s ease}.nuxt-lockup{position:relative;z-index:1;display:flex;align-items:flex-end;gap:calc(var(--nuxt-logo)/6);text-decoration:none;color:inherit}.nuxt-logo-slot{width:var(--nuxt-logo);height:calc(var(--nuxt-logo)*32/48);flex:0 0 var(--nuxt-logo);color:var(--nuxt-mint)}.nuxt-logo-slot>svg{width:100%;height:100%}.nuxt-logo-slot>svg>path{fill:currentColor}.nuxt-wordmark{width:calc(var(--nuxt-logo)*1.5);height:calc(var(--nuxt-logo)*.5);color:#fff;transition:color .2s ease}.nuxt-lockup:hover .nuxt-wordmark{color:var(--nuxt-mist)}.nuxt-corners{position:fixed;top:0;left:0;right:0;z-index:1;display:flex;justify-content:space-between;padding:22px 26px;pointer-events:none;font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:11px;font-weight:500;font-variant-numeric:tabular-nums;letter-spacing:.12em;line-height:1}.nuxt-home{color:var(--nuxt-dim);text-decoration:none;pointer-events:auto;transition:color .2s ease}.nuxt-home:hover{color:var(--nuxt-mist)}.nuxt-version{color:var(--nuxt-accent)}.nuxt-loader-bar{position:fixed;bottom:0;left:0;right:0;z-index:1;height:2px;background:var(--nuxt-mint)}.nuxt-season{position:fixed;right:22px;bottom:26px;z-index:1;display:flex;align-items:center;justify-content:center;width:30px;height:30px;padding:0;color:var(--nuxt-mint);background:rgba(0,220,130,.08);border:1px solid rgba(0,220,130,.24);border-radius:8px;cursor:pointer;transition:opacity .2s ease,background-color .2s ease}.nuxt-season[hidden]{display:none}.nuxt-season:hover{background:rgba(0,220,130,.16)}.nuxt-season>svg{width:16px;height:16px}.nuxt-sr{position:absolute;width:1px;height:1px;overflow:hidden;clip-path:inset(50%);white-space:nowrap}@media (prefers-color-scheme:light){:root{--nuxt-mist:rgba(2,4,32,.76);--nuxt-dim:rgba(2,4,32,.42);--nuxt-accent:#00a86a}body,html{background:#fff}.nuxt-wordmark{color:#020420}}</style><script>var e=.55;function t(e,n,t,o){let a=1-Math.exp(-Math.min(Math.max(t,0),.05)/Math.max(o,.001));return[e[0]+(n[0]-e[0])*a,e[1]+(n[1]-e[1])*a]}function n(){try{let e=new URLSearchParams(window.location.search).get(\"winter\");if(null!==e)return\"0\"!==e&&\"false\"!==e}catch{}let e=new Date,n=e.getFullYear(),t=-new Date(n,0,1).getTimezoneOffset(),o=-new Date(n,6,1).getTimezoneOffset();return t!==o&&(t-o>0?[4,5,6]:[10,11,0]).includes(e.getMonth())}function r(o,a,r){let i,s,l,c,h,d,f=!1,u=0,p=1,v=1,m=[.5,.5],g=[.1,.1],w=[.5,.5],x=0,y=!1,E=0,L=0,k=0,b=-1/0,A=!1,P=!1,S=!1,R=!1,M=0,C=0,_=0,O=window.matchMedia(\"(prefers-reduced-motion: reduce)\").matches,N=window.matchMedia(\"(prefers-color-scheme: light)\"),z=+!!N.matches,T=+!!n(),F=()=>{f||P||!A||(b=-1/0,u||=requestAnimationFrame(B))},I=e=>{z=+!!e.matches,W(),F()},W=()=>{i&&(z>0?i.blendFuncSeparate(i.ONE,i.ONE_MINUS_SRC_ALPHA,i.ZERO,i.ONE):i.blendFuncSeparate(i.ONE,i.ONE,i.ONE,i.ONE))},q=()=>{if(!i)return;let e=o.getBoundingClientRect();if(e.width<=0||e.height<=0)return;let n=Math.min(window.devicePixelRatio||1,1.75,2688/e.width),t=[Math.max(1,Math.floor(e.width*n)),Math.max(1,Math.floor(e.height*n))];(t[0]!==p||t[1]!==v)&&(p=t[0],v=t[1],o.width=p,o.height=v,i.viewport(0,0,p,v));let r=a.getBoundingClientRect();m=[(r.left+r.width/2-e.left)/Math.max(1,e.width),(r.top+r.height/2-e.top)/Math.max(1,e.height)],g=[Math.max(.001,r.width/Math.max(1,e.width)),Math.max(.001,r.height/Math.max(1,e.height))]},D=e=>{if(\"touch\"===e.pointerType)return;let n=o.getBoundingClientRect();c=[Math.min(1,Math.max(0,(e.clientX-n.left)/Math.max(1,n.width))),Math.min(1,Math.max(0,(e.clientY-n.top)/Math.max(1,n.height)))]},H=()=>{c=void 0,y=!1},K=()=>{y=!0},U=()=>{y=!1},Z=e=>{e.preventDefault(),Y(),o.remove()},X=e=>{if(e!==P&&!f){if(e&&!A)return void(S=!0);if(P=e,P)return M=C,c=void 0,y=!1,u&&cancelAnimationFrame(u),void(u=0);S=!1,R=!0,u||=requestAnimationFrame(B)}},B=n=>{if(f||(u=requestAnimationFrame(B),C=n,R&&(R=!1,_+=n-M,b=-1/0,k=(n-_)/1e3),n-b<33)||!i||!s||!h)return;b=n;let o=(n-_)/1e3,a=Math.min(Math.max(o-k,0),.05);k=o,w=t(w,c??w,a,.22),x+=(+!!c-x)*(1-Math.exp(-a/.35)),y?(L=1,E=(E+a*e)%1):L>0&&(E+=a*e,E>=1&&(E=0,L=0)),i.uniform2f(h.uLogoCentre,m[0],m[1]),i.uniform2f(h.uLogoScale,g[0],g[1]),i.uniform2f(h.uResolution,p,v),i.uniform2f(h.uPointer,w[0],w[1]),i.uniform2f(h.uWave,E,L),i.uniform2f(h.uMisc,o,x),i.uniform2f(h.uSeason,T,z),i.uniform1f(h.uLight,z);let r=+(z>0);i.clearColor(r,r,r,1),i.clear(i.COLOR_BUFFER_BIT),i.drawArrays(i.TRIANGLES,0,T>0?1111440:1098240),A||(A=!0,j.onFirstFrame?.(),S&&X(!0)),O&&(cancelAnimationFrame(u),u=0)},Y=()=>{f||(f=!0,u&&cancelAnimationFrame(u),u=0,l?.disconnect(),window.removeEventListener(\"pointermove\",D),document.documentElement.removeEventListener(\"pointerleave\",H),r?.removeEventListener(\"pointerenter\",K),r?.removeEventListener(\"pointerleave\",U),N.removeEventListener(\"change\",I),o.removeEventListener(\"webglcontextlost\",Z),i&&s&&i.deleteProgram(s),i=void 0,s=void 0)},V=(e,n,t)=>{let o=e.createShader(n);if(!o)throw Error(\"Could not create shader.\");if(e.shaderSource(o,t),e.compileShader(o),!e.getShaderParameter(o,e.COMPILE_STATUS)){let n=e.getShaderInfoLog(o);throw e.deleteShader(o),Error(`Shader failed to compile: ${n}`)}return o};try{(()=>{let e=o.getContext(\"webgl2\",{alpha:!1,antialias:!1,depth:!1,stencil:!1,powerPreference:\"low-power\"});if(!e)throw Error(\"No WebGL2 context.\");i=e;let n=V(e,e.VERTEX_SHADER,\"#version 300 es\\n// A particle mountain range for the dev-server loading screen.\\n//\\n// One draw call and no vertex buffers: every particle is a quad whose position\\n// is derived from gl_VertexID, sampled from a ridged heightfield in\\n// perspective, so the field reads as a range of peaks seen at a grazing angle.\\n// Nothing here depends on build progress — the screen looks the same at one\\n// second and at two minutes.\\n//\\n// Motion, all continuous loops:\\n//   · per-particle drift and shimmer\\n//   · slow luminosity waves crossing the field\\n//   · a ripple radiating from the lockup\\n//   · a shockwave while the pointer is over the lockup, phase driven by the CPU\\nprecision highp float;\\n\\nuniform vec2 uLogoCentre;\\nuniform vec2 uLogoScale;\\nuniform vec2 uResolution;\\n/** pointer position in canvas space, smoothed */\\nuniform vec2 uPointer;\\n/** x: shockwave phase 0..1, y: 1 while a wave is travelling */\\nuniform vec2 uWave;\\n/** x: seconds, y: pointer presence 0..1 */\\nuniform vec2 uMisc;\\n/** x: winter 0..1 — snow on the peaks and falling flakes; y: 1 in light mode */\\nuniform vec2 uSeason;\\n\\nout vec2 vLocal;\\n/** dark: emitted colour, w unused. light: ink colour, w its coverage. */\\nout vec4 vTint;\\n\\nconst vec3 MINT = vec3(0.0, 0.8627451, 0.5098039);\\nconst vec3 MINT_PALE = vec3(0.6235294, 1.0, 0.8156863);\\nconst vec3 SNOW = vec3(0.88, 1.0, 0.96);\\n\\n// Light mode draws the same range as ink on paper. Emitted light becomes\\n// coverage, and the renderer blends it over the page instead of adding it to\\n// the dark. Alpha rather than subtraction, so a crest that many particles\\n// land on settles on the brand green instead of running away to black.\\nconst vec3 INK_SOFT = vec3(0.6588235, 0.7882353, 0.7294118);\\nconst vec3 INK_DEEP = vec3(0.0, 0.8627451, 0.5098039);\\nconst vec3 FLAKE_INK = vec3(0.5215687, 0.6039216, 0.6588235);\\nconst vec3 LUMA = vec3(0.2126, 0.7152, 0.0722);\\n\\n// grid of particles\\nconst uint COLS = 704u;\\nconst uint ROWS = 260u;\\nconst uint TERRAIN_COUNT = COLS * ROWS;\\n\\n// camera\\nconst float Z_NEAR = 1.3;\\nconst float Z_FAR = 26.0;\\nconst float CAM_Y = 2.4;\\nconst float FOCAL = 1.3;\\nconst float HORIZON = 0.30;\\n\\n// the two summits that shape the skyline, in screen-x units\\nconst float PEAK1_X = -0.28;\\nconst float PEAK1_H = 3.2;\\nconst float PEAK2_X = 0.62;\\nconst float PEAK2_H = 2.2;\\n\\nconst vec2 QUAD[6] = vec2[6](\\n  vec2(-1.0, -1.0), vec2(1.0, -1.0), vec2(-1.0, 1.0),\\n  vec2(-1.0, 1.0), vec2(1.0, -1.0), vec2(1.0, 1.0)\\n);\\n\\nfloat hash21 (vec2 p) {\\n  vec2 q = fract(p * vec2(0.1031, 0.1030));\\n  q += dot(q, q.yx + 33.33);\\n  return fract((q.x + q.y) * q.x);\\n}\\n\\nfloat noise2 (vec2 p) {\\n  vec2 i = floor(p);\\n  vec2 f = fract(p);\\n  vec2 u = f * f * (3.0 - 2.0 * f);\\n  float a = hash21(i);\\n  float b = hash21(i + vec2(1.0, 0.0));\\n  float c = hash21(i + vec2(0.0, 1.0));\\n  float d = hash21(i + vec2(1.0, 1.0));\\n  return mix(mix(a, b, u.x), mix(c, d, u.x), u.y);\\n}\\n\\n// `shape` flattens the relief on narrow screens. A summit covers the same\\n// fraction of screen height whatever the aspect, but far fewer pixels of width\\n// in portrait, so without this the peaks turn into needles.\\nfloat terrain (float x, float z, float u, float shape) {\\n  // ridged noise stretched along the view axis gives crest lines that flow\\n  // into the distance rather than a lumpy field\\n  vec2 q = vec2(x * 0.30, z * 0.085);\\n  float r = 0.0;\\n  float amp = 0.55;\\n  mat2 rot = mat2(1.6, 1.2, -1.2, 1.6);\\n  for (int i = 0; i < 5; i++) {\\n    r += amp * (1.0 - abs(2.0 * noise2(q) - 1.0));\\n    q = rot * q;\\n    amp *= 0.55;\\n  }\\n  float h = pow(max(r, 0.0), 1.5) * 2.6;\\n  h *= 0.70 + 0.60 * noise2(vec2(x * 0.05, z * 0.05) + 7.0);\\n  // two designed summits, far enough back to break the horizon\\n  float rise = smoothstep(3.0, 9.0, z);\\n  h += rise * PEAK1_H * max(0.0, 1.0 - abs(u - PEAK1_X) * 3.2);\\n  h += rise * PEAK2_H * max(0.0, 1.0 - abs(u - PEAK2_X) * 3.6);\\n  return max(h, 0.0) * shape;\\n}\\n\\nvoid main () {\\n  uint id = uint(gl_VertexID) / 6u;\\n  int corner = gl_VertexID % 6;\\n  float t = uMisc.x;\\n  vec2 resolution = max(uResolution, vec2(1.0));\\n  float aspect = max(resolution.x / resolution.y, 0.5);\\n  float winter = uSeason.x;\\n  float light = uSeason.y;\\n\\n  // the lockup centre is needed by both the terrain and the flakes\\n  float logoH = max(uLogoScale.y, 0.01);\\n  vec2 centre = vec2(\\n    uLogoCentre.x * 2.0 - 1.0 + logoH * 2.5 / aspect,\\n    1.0 - 2.0 * uLogoCentre.y\\n  );\\n  vec2 offset = QUAD[corner];\\n\\n  // A flake drifting down the screen, in its own parallax layer. Independent of\\n  // the terrain: it lives in screen space and wraps around forever. Flakes are\\n  // drawn from the indices past the terrain, so winter costs nothing out of\\n  // season — the draw call is simply shorter.\\n  if (id >= TERRAIN_COUNT) {\\n    float fi = float(id - TERRAIN_COUNT);\\n    float r1 = hash21(vec2(fi, 1.7));\\n    float r2 = hash21(vec2(fi, 5.3));\\n    float r3 = hash21(vec2(fi, 9.1));\\n    // near flakes are bigger, brighter and fall faster\\n    float depth = 0.30 + r3 * 0.70;\\n    float x = (r1 * 2.4 - 1.2) + sin(t * (0.25 + r2 * 0.35) + r1 * 40.0) * 0.05 * depth;\\n    float y = 1.2 - fract(r2 + t * (0.020 + r3 * 0.035)) * 2.5;\\n    vec2 pos = vec2(x, y);\\n\\n    // flakes thin out over the lockup so they never sit on the wordmark\\n    vec2 toCentre = vec2((pos.x - centre.x) * aspect, pos.y - centre.y);\\n    vec2 clear = abs(toCentre / vec2(logoH * 7.0, logoH * 3.2));\\n    float clearing = 0.25 + 0.75 * smoothstep(0.5, 1.2, pow(pow(clear.x, 3.0) + pow(clear.y, 3.0), 1.0 / 3.0));\\n    float twinkle = 0.75 + 0.25 * sin(t * (1.1 + r1 * 2.0) + r2 * 30.0);\\n    float sizePx = (0.55 + depth * 1.5) * (resolution.y / 900.0);\\n    float amount = (0.16 + 0.48 * depth) * twinkle * clearing * winter;\\n\\n    gl_Position = vec4(pos + offset * sizePx * 2.0 / resolution, 0.5, 1.0);\\n    vLocal = offset;\\n    // white flakes cannot show on paper, so in light mode they are drawn as the\\n    // cool grey a flake reads as against a bright sky\\n    vTint = light > 0.5 ? vec4(FLAKE_INK, amount * 1.6) : vec4(SNOW * amount, 1.0);\\n    return;\\n  }\\n\\n  uint col = id % COLS;\\n  uint row = id / COLS;\\n  vec2 seed = vec2(float(col), float(row));\\n  float jitterX = hash21(seed) - 0.5;\\n  float jitterY = hash21(seed + 17.0) - 0.5;\\n  float rnd = hash21(seed + 41.0);\\n\\n  // screen-x is fixed per column; depth is biased so the near field is denser\\n  float u = ((float(col) + jitterX) / float(COLS)) * 2.3 - 1.15;\\n  float v = clamp((float(row) + jitterY) / float(ROWS), 0.0, 1.0);\\n  float z = mix(Z_NEAR, Z_FAR, pow(v, 1.5));\\n  // widen the field of view and flatten the range as the window narrows, so a\\n  // phone shows a range rather than a row of spikes\\n  float shape = clamp(aspect / 1.6, 0.5, 1.0);\\n  float tanX = max(aspect, 0.95) / FOCAL;\\n  float xw = u * z * tanX;\\n\\n  float h = terrain(xw, z, u, shape);\\n  h += (noise2(vec2(xw * 1.6, z * 1.6)) - 0.5) * 0.05 * shape;\\n\\n  // shading from the surface itself: one fixed light, plus a convexity term\\n  // that picks out the crests\\n  float e = 0.22;\\n  float hxp = terrain(xw + e, z, u, shape);\\n  float hxm = terrain(xw - e, z, u, shape);\\n  float hzp = terrain(xw, z + e, u, shape);\\n  vec3 normal = normalize(vec3(-(hxp - hxm) / (2.0 * e), 1.0, -(hzp - h) / e));\\n  vec3 lightDir = normalize(vec3(-0.12, 0.60, -0.42));\\n  float diffuse = pow(max(dot(normal, lightDir), 0.0), 1.3);\\n  float crest = smoothstep(0.0, 1.5, (2.0 * h - hxp - hxm) / (e * e) * 0.2);\\n\\n  float sy = (h - CAM_Y) / z * FOCAL + HORIZON;\\n\\n  // nearer ridges hide what lies behind them, which is what makes the skyline\\n  float occluded = 0.0;\\n  for (int k = 1; k <= 4; k++) {\\n    float zs = Z_NEAR + (z - Z_NEAR) * float(k) / 5.0;\\n    float hs = terrain(u * zs * tanX, zs, u, shape);\\n    occluded = max(occluded, step(sy + 0.012, (hs - CAM_Y) / zs * FOCAL + HORIZON));\\n  }\\n\\n  // the lockup sits in a clearing so it always reads on plain ground. Its\\n  // centre is right of the icon slot: the official mark is 128 units wide for a\\n  // 48-unit icon, so the middle is 1.25 icon-heights along.\\n  vec2 toCentre = vec2((u - centre.x) * aspect, sy - centre.y);\\n  float dist = length(toCentre);\\n  // The clearing has to read as haze, not as a hole cut out of the scene, so it\\n  // fades over a long distance and its edge is broken up by noise. A squircle\\n  // rather than an ellipse: the lockup is a wide, short block, which an ellipse\\n  // fits poorly.\\n  vec2 clear = abs(toCentre / vec2(logoH * 8.0, logoH * 3.6));\\n  float clearEdge = pow(pow(clear.x, 3.0) + pow(clear.y, 3.0), 1.0 / 3.0)\\n    + (noise2(vec2(xw * 0.55, z * 0.55)) - 0.5) * 0.30;\\n  float clearing = smoothstep(0.46, 1.30, clearEdge);\\n\\n  // ambient ripple, always running\\n  float ripplePhase = fract(t * 0.30);\\n  float rippleRadius = ripplePhase * 2.4;\\n  float rippleWidth = 0.16 + ripplePhase * 0.25;\\n  float ripple = exp(-((dist - rippleRadius) * (dist - rippleRadius)) / (rippleWidth * rippleWidth))\\n    * (1.0 - smoothstep(0.55, 0.95, ripplePhase))\\n    * smoothstep(0.0, 0.05, ripplePhase);\\n\\n  // shockwave from hovering the lockup; phase comes from the CPU so a wave\\n  // always finishes travelling once fired\\n  float hoverPhase = uWave.x;\\n  float hoverRadius = hoverPhase * 1.7;\\n  float hoverWidth = 0.09 + hoverPhase * 0.20;\\n  float shock = exp(-((dist - hoverRadius) * (dist - hoverRadius)) / (hoverWidth * hoverWidth))\\n    * (1.0 - smoothstep(0.55, 1.0, hoverPhase))\\n    * smoothstep(0.0, 0.04, hoverPhase)\\n    * uWave.y;\\n\\n  float fog = exp(-(z - Z_NEAR) * 0.075) * smoothstep(1.0, 0.86, v);\\n  float nearFade = smoothstep(Z_NEAR, 2.6, z);\\n  // in winter the snowline drops a long way and the caps turn properly white\\n  float snowline = 2.6 - winter * 1.7;\\n  float snow = smoothstep(snowline, snowline + 1.0 - winter * 0.45, h);\\n  float shimmer = 0.88 + 0.12 * sin(t * (0.7 + rnd * 1.4) + rnd * 31.0);\\n  float grain = 0.88 + 0.24 * noise2(vec2(xw * 3.0, z * 3.0));\\n\\n  // a fifth of the particles pulse on their own cycle, so the field never\\n  // settles into a repeating pattern\\n  float pulses = step(0.82, hash21(seed + 63.0));\\n  float pulse = pow(0.5 + 0.5 * sin(t * (0.5 + rnd * 1.1) + rnd * 61.0), 6.0);\\n\\n  // two slow noise waves drifting across the field\\n  float waveA = noise2(vec2(xw * 0.22 - t * 0.20, z * 0.22)) - 0.5;\\n  float waveB = noise2(vec2(xw * 0.55 + t * 0.09, z * 0.55 - t * 0.14)) - 0.5;\\n  float wave = 1.0 + waveA * 0.24 + waveB * 0.16;\\n\\n  // snow throws light back, so the caps read brighter as well as whiter\\n  float lit = diffuse * 0.18 + crest * (0.34 + diffuse * 0.45) + snow * (0.06 + winter * 0.30);\\n  float brightness = (0.008 + lit * grain)\\n    * fog * nearFade * wave * shimmer\\n    * (1.0 + pulse * (0.18 + pulses * 1.1))\\n    * (1.0 + ripple * 0.75 + shock * 3.0);\\n  vec3 tint = mix(MINT * 0.75, MINT_PALE, clamp(crest + snow * 0.5, 0.0, 1.0));\\n  tint = mix(tint, SNOW, winter * snow) * brightness;\\n\\n  float glint = step(0.993, rnd) * diffuse * (0.5 + 0.5 * sin(t * 2.0 + rnd * 47.0));\\n  tint += vec3(0.85, 1.0, 0.93) * glint * fog * 0.55;\\n\\n  // the pointer stirs nearby dust\\n  vec2 pointer = vec2((uPointer.x - 0.5) * 2.0, (0.5 - uPointer.y) * 2.0);\\n  vec2 toPointer = vec2((u - pointer.x) * aspect, sy - pointer.y);\\n  float excite = smoothstep(0.55, 0.0, length(toPointer)) * uMisc.y;\\n  vec2 push = normalize(toPointer + vec2(0.0001, 0.0)) * excite * (0.006 + 0.010 * rnd);\\n  tint *= 1.0 + excite * 1.3;\\n\\n  // Light mode reuses every lighting term above and only reinterprets it: how\\n  // much light a particle emits becomes how much ink it lays down. Taking the\\n  // luminance keeps the two themes at matching density, and the crests that go\\n  // palest on black carry the brand green on paper. Snow is the one term that\\n  // has to invert — a white cap can only read on paper by leaving it bare.\\n  if (light > 0.5) {\\n    // lifted, since faint dust that reads fine against black all but vanishes\\n    // against white\\n    float coverage = pow(clamp(dot(tint, LUMA) * 2.2, 0.0, 1.0), 0.8) * (1.0 - winter * snow * 0.6);\\n    vec3 ink = mix(INK_SOFT, INK_DEEP, clamp(crest * 1.15 + snow * 0.3, 0.0, 1.0));\\n    vTint = vec4(ink, coverage * (1.0 - occluded) * clearing);\\n  } else {\\n    vTint = vec4(tint * (1.0 - occluded) * clearing, 1.0);\\n  }\\n\\n  // particles float around their anchor, more freely up close\\n  float floatAmp = (0.0015 + (1.0 - v) * 0.006) * (1.0 + excite * 2.5);\\n  vec2 drift = vec2(\\n    sin(t * (0.35 + rnd * 0.5) + rnd * 40.0),\\n    cos(t * (0.28 + rnd * 0.45) + rnd * 23.0) * 1.4\\n  ) * floatAmp;\\n\\n  // the shockwave shoves particles outward as it passes\\n  vec2 shockPush = vec2(toCentre.x / max(dist, 0.02) / aspect, toCentre.y / max(dist, 0.02)) * shock * 0.045;\\n\\n  float sizePx = clamp(16.0 / z, 0.65, 1.9)\\n    * (0.75 + 0.5 * hash21(seed + 27.0))\\n    * (1.0 + glint * 0.25 + ripple * 0.20 + shock * 0.75);\\n\\n  gl_Position = vec4(\\n    vec2(u, sy) + drift + push + shockPush + offset * sizePx * 2.0 / resolution,\\n    0.5,\\n    1.0\\n  );\\n  vLocal = offset;\\n}\\n\"),t=V(e,e.FRAGMENT_SHADER,\"#version 300 es\\nprecision highp float;\\n\\nin vec2 vLocal;\\nin vec4 vTint;\\n/** 1 in light mode; uniform across the draw, so the branch is free */\\nuniform float uLight;\\nout vec4 fragColor;\\n\\nvoid main () {\\n  float falloff = smoothstep(1.0, 0.45, length(vLocal));\\n  if (uLight > 0.5) {\\n    // premultiplied, so it composites over the page and settles on the ink\\n    // colour however much dust lands on it\\n    float alpha = vTint.a * falloff;\\n    fragColor = vec4(vTint.rgb * alpha, alpha);\\n  } else {\\n    fragColor = vec4(vTint.rgb * falloff, 1.0);\\n  }\\n}\\n\"),c=e.createProgram();if(!c)throw Error(\"Could not create program.\");if(e.attachShader(c,n),e.attachShader(c,t),e.linkProgram(c),e.deleteShader(n),e.deleteShader(t),!e.getProgramParameter(c,e.LINK_STATUS)){let n=e.getProgramInfoLog(c);throw e.deleteProgram(c),Error(`Program failed to link: ${n}`)}s=c,e.useProgram(c);let d=n=>e.getUniformLocation(c,n);h={uLogoCentre:d(\"uLogoCentre\"),uLogoScale:d(\"uLogoScale\"),uResolution:d(\"uResolution\"),uPointer:d(\"uPointer\"),uWave:d(\"uWave\"),uMisc:d(\"uMisc\"),uSeason:d(\"uSeason\"),uLight:d(\"uLight\")},e.bindVertexArray(e.createVertexArray()),e.disable(e.DEPTH_TEST),e.enable(e.BLEND),W(),q(),l=typeof ResizeObserver>\"u\"?void 0:new ResizeObserver(q),l?.observe(o),l?.observe(a),window.addEventListener(\"pointermove\",D),document.documentElement.addEventListener(\"pointerleave\",H),r?.addEventListener(\"pointerenter\",K),r?.addEventListener(\"pointerleave\",U),N.addEventListener(\"change\",I),o.addEventListener(\"webglcontextlost\",Z),u||=requestAnimationFrame(B)})(),d=Promise.resolve()}catch(e){Y(),d=Promise.reject(e instanceof Error?e:Error(String(e)))}let j={ready:d,dispose:Y,setPaused:X,setWinter:e=>{let n=+!!e;n!==T&&(T=n,F())}};return j}var i=\"nuxt-snow\";function a(){try{return\"false\"!==window.localStorage.getItem(i)}catch{return!0}}function o(e){let t=n(),o=a();if(e.setWinter(t&&o),!t)return;let r=document.querySelector(\"#nuxt-season\");if(!r)return;let s=()=>{r.style.opacity=o?\"1\":\"0.4\",r.setAttribute(\"aria-pressed\",String(o)),r.title=o?\"Turn off snow\":\"Turn on snow\"};s(),r.hidden=!1,r.addEventListener(\"click\",()=>{o=!o,e.setWinter(o),s();try{window.localStorage.setItem(i,String(o))}catch{}})}function s(){try{let e=new URLSearchParams(window.location.search).get(\"shader\");return\"0\"===e||\"false\"===e}catch{return!1}}function c(){let e=document.querySelector(\"#nuxt-shader\"),n=document.querySelector(\"#nuxt-logo-slot\");if(!e||!n||!window.WebGL2RenderingContext||s())return void e?.remove();let t=r(e,n,document.querySelector(\".nuxt-lockup\"));o(t),t.onFirstFrame=()=>{e.style.opacity=\"1\"},t.ready.catch(()=>e.remove()),window.addEventListener(\"pagehide\",()=>t.dispose(),{once:!0});let a=e=>t.setPaused(e);window.addEventListener(\"blur\",()=>a(!0)),window.addEventListener(\"focus\",()=>a(!1)),document.addEventListener(\"visibilitychange\",()=>{document.hidden?a(!0):document.hasFocus()&&a(!1)}),(document.hidden||!document.hasFocus())&&a(!0)}\"loading\"===document.readyState?document.addEventListener(\"DOMContentLoaded\",c,{once:!0}):c();</script></head><body><canvas id=\"nuxt-shader\" aria-hidden=\"true\"></canvas><a href=\"https://nuxt.com/?utm_source=nuxt-loading-screen\" target=\"_blank\" rel=\"noopener\" class=\"nuxt-lockup\" id=\"nuxtImg\" aria-label=\"" + escapeHtml(messages.appName) + "\"> <span id=\"nuxt-logo-slot\" class=\"nuxt-logo-slot\"><svg xmlns=\"http://www.w3.org/2000/svg\" fill=\"none\" aria-hidden=\"true\" viewBox=\"0 0 48 32\"><path d=\"M26.88 32h17.76c.567 0 1.11-.2 1.6-.48s.997-.633 1.28-1.12c.283-.486.48-1.038.48-1.6s-.196-1.114-.48-1.6l-12-20.64a2.9 2.9 0 0 0-1.12-1.12c-.49-.28-1.193-.48-1.76-.48s-1.11.2-1.6.48-.837.634-1.12 1.12l-3.04 5.28L20.8 1.6c-.284-.487-.63-1-1.12-1.28S18.647 0 18.08 0s-1.11.04-1.6.32-.997.793-1.28 1.28L.32 27.2c-.284.486-.32 1.038-.32 1.6 0 .561.037 1.113.32 1.6.283.486.79.84 1.28 1.12s1.033.48 1.6.48h11.2c4.438 0 7.668-1.99 9.92-5.76l5.44-9.44 2.88-4.96 8.8 15.04H29.76zm-12.64-5.12H6.4L18.08 6.72 24 16.8l-3.921 6.836c-1.496 2.446-3.201 3.244-5.839 3.244\"/></svg> </span> <svg xmlns=\"http://www.w3.org/2000/svg\" fill=\"none\" aria-hidden=\"true\" class=\"nuxt-wordmark\" viewBox=\"56 8 72 24\"><path fill=\"currentColor\" d=\"M60.32 32c.346 0 .64-.287.64-.64V16.48s.8 1.44 2.24 3.84l6.24 10.72c.285.598.919.96 1.44.96h4.32V8h-4.32c-.288 0-.64.24-.64.64v15.04l-2.88-5.12L61.6 8.8c-.28-.497-.883-.8-1.44-.8H56v24zm55.84-17.28h2.08c.53 0 .96-.43.96-.96V9.6h4.48v5.12H128v3.84h-4.32v6.88c0 1.68.809 2.4 2.24 2.4H128V32h-2.72c-3.688 0-6.08-2.389-6.08-6.4v-7.04h-3.04zm-21.6 0v9.92c0 2.24-.841 4.13-2.08 5.44S89.53 32 87.2 32s-4.201-.61-5.44-1.92c-1.218-1.31-2.08-3.2-2.08-5.44v-9.92h2.56c.546 0 1.083.1 1.44.48.357.36.48.57.48 1.12v8.32c0 1.29.073 2.086.64 2.72.567.613 1.266.8 2.4.8 1.155 0 1.673-.187 2.24-.8.567-.634.64-1.43.64-2.72v-8.32c0-.55.123-.9.48-1.28.314-.334.644-.326 1.12-.32zm13.6 8.32 5.44-8.32h-4.16c-.524 0-.99.188-1.28.64l-2.56 3.84-2.4-3.68c-.29-.452-.916-.8-1.44-.8h-4l5.44 8.16L97.28 32h4.16c.52 0 .989-.514 1.28-.96l2.88-4.32 3.04 4.48c.291.446.76.8 1.28.8h4.16z\"/></svg> </a><div class=\"nuxt-corners\"><a class=\"nuxt-home\" href=\"https://nuxt.com/?utm_source=nuxt-loading-screen\" target=\"_blank\" rel=\"noopener\">nuxt.com</a> <span class=\"nuxt-version\">5.0.0-0</span></div> <button class=\"nuxt-season\" id=\"nuxt-season\" type=\"button\" hidden aria-pressed=\"true\" title=\"Turn off snow\"><svg xmlns=\"http://www.w3.org/2000/svg\" fill=\"none\" stroke=\"currentColor\" stroke-linecap=\"round\" stroke-linejoin=\"round\" stroke-width=\"1.3\" aria-hidden=\"true\" viewBox=\"0 0 16 16\"><path d=\"M8 1.6v12.8M2.46 4.8l11.08 6.4m-11.08 0 11.08-6.4M6.1 3.1 8 4.2l1.9-1.1m-3.8 9.8L8 11.8l1.9 1.1\"/></svg> <span class=\"nuxt-sr\">Snow</span></button> <div class=\"nuxt-loader-bar\"></div><script>if(void 0===window.fetch)setTimeout(()=>window.location.reload(),200);else{let t=200;const e=async()=>{try{if(!(await window.fetch(window.location.href,{headers:{accept:\"text/html\"}}).then(t=>t.text())).includes(\"__NUXT_LOADING__\"))return window.location.reload()}catch{}setTimeout(e,t),t=Math.min(Math.round(1.5*t),1e3)};e()}</script></body></html>"
}