// This file is generated by @nuxt/ui-templates and committed to the repository.
// Do not edit it directly: edit the source in packages/ui-templates/templates/
// and run `pnpm --filter @nuxt/ui-templates build`.

import { escapeHtml } from '@vue/shared'

export type DefaultMessages = Record<"appName" | "loading" | "version", string | boolean | number >
const _messages = {"appName":"Nuxt","loading":"Loading","version":"4.0"}
export const template = (messages: Partial<DefaultMessages>): string => {
  messages = { ..._messages, ...messages }
  return "<!DOCTYPE html><html lang=\"en\"><head><title>" + escapeHtml(messages.loading) + " | " + escapeHtml(messages.appName) + "</title><meta charset=\"utf-8\"><meta content=\"width=device-width,initial-scale=1,minimum-scale=1\" name=\"viewport\"><style>:root{--nuxt-mint:#00dc82;--nuxt-mist:rgba(232,255,244,.88);--nuxt-dim:rgba(0,220,130,.55);--nuxt-logo:min(15vw, 99px)}*,:after,:before{box-sizing:border-box}body,html{margin:0;width:100%;height:100%;background:#000;color:var(--nuxt-mist)}body{display:flex;align-items:center;justify-content:center;overflow:hidden;font-family:ui-sans-serif,system-ui,sans-serif;-webkit-font-smoothing:antialiased}#nuxt-shader,svg{display:block}#nuxt-shader{position:fixed;inset:0;width:100%;height:100%;z-index:0;touch-action:none;opacity:0;transition:opacity .4s ease}.nuxt-lockup{position:relative;z-index:1;display:flex;align-items:flex-end;gap:calc(var(--nuxt-logo)/6);text-decoration:none;color:inherit}.nuxt-logo-slot{width:var(--nuxt-logo);height:calc(var(--nuxt-logo)*32/48);flex:0 0 var(--nuxt-logo);color:var(--nuxt-mint)}.nuxt-logo-slot>svg{width:100%;height:100%}.nuxt-logo-slot>svg>path{fill:currentColor}.nuxt-wordmark{width:calc(var(--nuxt-logo)*1.5);height:calc(var(--nuxt-logo)*.5);color:#fff;transition:color .2s ease}.nuxt-lockup:hover .nuxt-wordmark{color:var(--nuxt-mist)}.nuxt-corners{position:fixed;top:0;left:0;right:0;z-index:1;display:flex;justify-content:space-between;padding:22px 26px;pointer-events:none;font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:11px;font-weight:500;font-variant-numeric:tabular-nums;letter-spacing:.12em;line-height:1}.nuxt-home{color:var(--nuxt-dim);text-decoration:none;pointer-events:auto;transition:color .2s ease}.nuxt-home:hover{color:var(--nuxt-mist)}.nuxt-version{color:var(--nuxt-mint)}.nuxt-progress{position:fixed;bottom:0;left:0;right:0;z-index:1}.nuxt-progress-status{margin:0 0 14px;text-align:center;font-family:ui-monospace,SFMono-Regular,Menlo,monospace;font-size:11px;font-weight:500;font-variant-numeric:tabular-nums;letter-spacing:.08em;line-height:1;color:var(--nuxt-dim);opacity:0;transition:opacity .3s ease}.nuxt-loader-bar{height:2px;overflow:hidden;background:rgba(0,220,130,.16)}.nuxt-loader-bar>span{display:block;height:100%;width:32%;background:var(--nuxt-mint);transform:translateX(-120%);animation:nuxt-bar 1.8s ease-in-out infinite}@keyframes nuxt-bar{0%{transform:translateX(-120%)}to{transform:translateX(360%)}}@media (prefers-reduced-motion:reduce){.nuxt-loader-bar>span{width:100%;transform:none;animation:none}}</style><script>import\"./modulepreload-polyfill-P2Xu9kJm.js\";var e=\"__NUXT_LOADING_STATE__\",t=\"nuxt:loading-state\";function n(e){if(!e||\"object\"!=typeof e)return;let t=e,n=t.step;if(!n||\"string\"!=typeof n.name)return;let a=Math.max(1,Math.round(n.total)),o=Math.min(a,Math.max(1,Math.round(n.index)));return{step:{name:n.name,index:o,total:a},startedAt:\"number\"==typeof t.startedAt?t.startedAt:Date.now()}}function r(e){let t=e/1e3;return t<10?`${t.toFixed(1)}s`:`${Math.round(t)}s`}function i(){let a=document.querySelector(\"#nuxt-progress\"),o=document.querySelector(\"#nuxt-progress-status\");if(!a||!o)return;let i,s=0,l=()=>{if(!i?.step)return;let{name:e,index:t,total:n}=i.step,a=r(Date.now()-(i.startedAt??Date.now()));o.textContent=\"\";let s=document.createElement(\"span\");s.className=\"nuxt-progress-step\",s.textContent=e,o.append(s,` · step ${t}/${n} · ${a}`)},c=()=>{l(),s=window.setTimeout(c,100)},h=e=>{if(i=n(e),!i?.step)return a.removeAttribute(\"data-mode\"),window.clearTimeout(s),void(s=0);a.dataset.mode=\"determinate\",a.style.setProperty(\"--nuxt-progress\",i.step.index/i.step.total*100+\"%\"),l(),s||c()};h(window[e]),window.addEventListener(t,e=>h(e.detail))}var a=\"// A particle mountain range for the dev-server loading screen.\\n//\\n// One draw call: every particle is a quad whose position comes from a ridged\\n// heightfield sampled in perspective, so the field reads as a range of peaks\\n// seen at a grazing angle. Nothing here depends on build progress — the screen\\n// looks the same at one second and at two minutes.\\n//\\n// Motion, all continuous loops:\\n//   · per-particle drift and shimmer\\n//   · slow luminosity waves crossing the field\\n//   · a ripple radiating from the lockup\\n//   · a shockwave while the pointer is over the lockup, phase driven by the CPU\\n\\nstruct Params {\\n  logoCentre: vec2f,\\n  logoScale: vec2f,\\n  resolution: vec2f,\\n  /** pointer position in canvas space, smoothed */\\n  pointer: vec2f,\\n  /** x: shockwave phase 0..1, y: 1 while a wave is travelling */\\n  wave: vec2f,\\n  /** x: seconds, y: pointer presence 0..1 */\\n  misc: vec2f,\\n}\\n@group(0) @binding(0) var<uniform> params: Params;\\n\\nconst MINT = vec3f(0.0, 0.8627451, 0.5098039);\\nconst MINT_PALE = vec3f(0.6235294, 1.0, 0.8156863);\\n\\n// grid of particles\\nconst COLS = 704u;\\nconst ROWS = 260u;\\n\\n// camera\\nconst Z_NEAR = 1.3;\\nconst Z_FAR = 26.0;\\nconst CAM_Y = 2.4;\\nconst FOCAL = 1.3;\\nconst HORIZON = 0.30;\\n\\n// the two summits that shape the skyline, in screen-x units\\nconst PEAK1_X = -0.28;\\nconst PEAK1_H = 3.2;\\nconst PEAK2_X = 0.62;\\nconst PEAK2_H = 2.2;\\n\\nfn hash21 (p: vec2f) -> f32 {\\n  var q = fract(p * vec2f(0.1031, 0.1030));\\n  q += dot(q, q.yx + 33.33);\\n  return fract((q.x + q.y) * q.x);\\n}\\n\\nfn noise2 (p: vec2f) -> f32 {\\n  let i = floor(p);\\n  let f = fract(p);\\n  let u = f * f * (3.0 - 2.0 * f);\\n  let a = hash21(i);\\n  let b = hash21(i + vec2f(1.0, 0.0));\\n  let c = hash21(i + vec2f(0.0, 1.0));\\n  let d = hash21(i + vec2f(1.0, 1.0));\\n  return mix(mix(a, b, u.x), mix(c, d, u.x), u.y);\\n}\\n\\n// `shape` flattens the relief on narrow screens. A summit covers the same\\n// fraction of screen height whatever the aspect, but far fewer pixels of width\\n// in portrait, so without this the peaks turn into needles.\\nfn terrain (x: f32, z: f32, u: f32, shape: f32) -> f32 {\\n  // ridged noise stretched along the view axis gives crest lines that flow\\n  // into the distance rather than a lumpy field\\n  var q = vec2f(x * 0.30, z * 0.085);\\n  var r = 0.0;\\n  var amp = 0.55;\\n  let rot = mat2x2f(1.6, 1.2, -1.2, 1.6);\\n  for (var i = 0; i < 5; i++) {\\n    r += amp * (1.0 - abs(2.0 * noise2(q) - 1.0));\\n    q = rot * q;\\n    amp *= 0.55;\\n  }\\n  var h = pow(max(r, 0.0), 1.5) * 2.6;\\n  h *= 0.70 + 0.60 * noise2(vec2f(x * 0.05, z * 0.05) + 7.0);\\n  // two designed summits, far enough back to break the horizon\\n  let rise = smoothstep(3.0, 9.0, z);\\n  h += rise * PEAK1_H * max(0.0, 1.0 - abs(u - PEAK1_X) * 3.2);\\n  h += rise * PEAK2_H * max(0.0, 1.0 - abs(u - PEAK2_X) * 3.6);\\n  return max(h, 0.0) * shape;\\n}\\n\\nstruct Particle {\\n  @builtin(position) position: vec4f,\\n  @location(0) local: vec2f,\\n  @location(1) tint: vec3f,\\n}\\n\\n@vertex fn vs_main (@builtin(vertex_index) index: u32) -> Particle {\\n  let id = index / 6u;\\n  let corner = index % 6u;\\n  let col = id % COLS;\\n  let row = id / COLS;\\n  let t = params.misc.x;\\n  let resolution = max(params.resolution, vec2f(1.0));\\n  let aspect = max(resolution.x / resolution.y, 0.5);\\n\\n  let seed = vec2f(f32(col), f32(row));\\n  let jitterX = hash21(seed) - 0.5;\\n  let jitterY = hash21(seed + 17.0) - 0.5;\\n  let rnd = hash21(seed + 41.0);\\n\\n  // screen-x is fixed per column; depth is biased so the near field is denser\\n  let u = ((f32(col) + jitterX) / f32(COLS)) * 2.3 - 1.15;\\n  let v = clamp((f32(row) + jitterY) / f32(ROWS), 0.0, 1.0);\\n  let z = mix(Z_NEAR, Z_FAR, pow(v, 1.5));\\n  // widen the field of view and flatten the range as the window narrows, so a\\n  // phone shows a range rather than a row of spikes\\n  let shape = clamp(aspect / 1.6, 0.5, 1.0);\\n  let tanX = max(aspect, 0.95) / FOCAL;\\n  let xw = u * z * tanX;\\n\\n  var h = terrain(xw, z, u, shape);\\n  h += (noise2(vec2f(xw * 1.6, z * 1.6)) - 0.5) * 0.05 * shape;\\n\\n  // shading from the surface itself: one fixed light, plus a convexity term\\n  // that picks out the crests\\n  let e = 0.22;\\n  let hxp = terrain(xw + e, z, u, shape);\\n  let hxm = terrain(xw - e, z, u, shape);\\n  let hzp = terrain(xw, z + e, u, shape);\\n  let normal = normalize(vec3f(-(hxp - hxm) / (2.0 * e), 1.0, -(hzp - h) / e));\\n  let lightDir = normalize(vec3f(-0.12, 0.60, -0.42));\\n  let diffuse = pow(max(dot(normal, lightDir), 0.0), 1.3);\\n  let crest = smoothstep(0.0, 1.5, (2.0 * h - hxp - hxm) / (e * e) * 0.2);\\n\\n  let sy = (h - CAM_Y) / z * FOCAL + HORIZON;\\n\\n  // nearer ridges hide what lies behind them, which is what makes the skyline\\n  var occluded = 0.0;\\n  for (var k = 1; k <= 4; k++) {\\n    let zs = Z_NEAR + (z - Z_NEAR) * f32(k) / 5.0;\\n    let hs = terrain(u * zs * tanX, zs, u, shape);\\n    occluded = max(occluded, step(sy + 0.012, (hs - CAM_Y) / zs * FOCAL + HORIZON));\\n  }\\n\\n  // the lockup sits in a clearing so it always reads on plain black. Its centre\\n  // is right of the icon slot: the official mark is 128 units wide for a\\n  // 48-unit icon, so the middle is 1.25 icon-heights along.\\n  let logoH = max(params.logoScale.y, 0.01);\\n  let centre = vec2f(\\n    params.logoCentre.x * 2.0 - 1.0 + logoH * 2.5 / aspect,\\n    1.0 - 2.0 * params.logoCentre.y,\\n  );\\n  let toCentre = vec2f((u - centre.x) * aspect, sy - centre.y);\\n  let distance = length(toCentre);\\n  // The clearing has to read as haze, not as a hole cut out of the scene, so\\n  // it fades over a long distance and its edge is broken up by noise. A\\n  // squircle rather than an ellipse: the lockup is a wide, short block (half of\\n  // it is 4 logo-heights wide and 1 tall here), which an ellipse fits poorly.\\n  let clear = abs(toCentre / vec2f(logoH * 8.0, logoH * 3.6));\\n  let clearEdge = pow(pow(clear.x, 3.0) + pow(clear.y, 3.0), 1.0 / 3.0)\\n    + (noise2(vec2f(xw * 0.55, z * 0.55)) - 0.5) * 0.30;\\n  let clearing = smoothstep(0.46, 1.30, clearEdge);\\n\\n  // ambient ripple, always running\\n  let ripplePhase = fract(t * 0.30);\\n  let rippleRadius = ripplePhase * 2.4;\\n  let rippleWidth = 0.16 + ripplePhase * 0.25;\\n  let ripple = exp(-((distance - rippleRadius) * (distance - rippleRadius)) / (rippleWidth * rippleWidth))\\n    * (1.0 - smoothstep(0.55, 0.95, ripplePhase))\\n    * smoothstep(0.0, 0.05, ripplePhase);\\n\\n  // shockwave from hovering the lockup; phase comes from the CPU so a wave\\n  // always finishes travelling once fired\\n  let hoverPhase = params.wave.x;\\n  let hoverRadius = hoverPhase * 1.7;\\n  let hoverWidth = 0.09 + hoverPhase * 0.20;\\n  let shock = exp(-((distance - hoverRadius) * (distance - hoverRadius)) / (hoverWidth * hoverWidth))\\n    * (1.0 - smoothstep(0.55, 1.0, hoverPhase))\\n    * smoothstep(0.0, 0.04, hoverPhase)\\n    * params.wave.y;\\n\\n  let fog = exp(-(z - Z_NEAR) * 0.075) * smoothstep(1.0, 0.86, v);\\n  let nearFade = smoothstep(Z_NEAR, 2.6, z);\\n  let snow = smoothstep(2.6, 3.6, h);\\n  let shimmer = 0.88 + 0.12 * sin(t * (0.7 + rnd * 1.4) + rnd * 31.0);\\n  let grain = 0.88 + 0.24 * noise2(vec2f(xw * 3.0, z * 3.0));\\n\\n  // a fifth of the particles pulse on their own cycle, so the field never\\n  // settles into a repeating pattern\\n  let pulses = step(0.82, hash21(seed + 63.0));\\n  let pulse = pow(0.5 + 0.5 * sin(t * (0.5 + rnd * 1.1) + rnd * 61.0), 6.0);\\n\\n  // two slow noise waves drifting across the field\\n  let waveA = noise2(vec2f(xw * 0.22 - t * 0.20, z * 0.22)) - 0.5;\\n  let waveB = noise2(vec2f(xw * 0.55 + t * 0.09, z * 0.55 - t * 0.14)) - 0.5;\\n  let wave = 1.0 + waveA * 0.24 + waveB * 0.16;\\n\\n  let lit = diffuse * 0.18 + crest * (0.34 + diffuse * 0.45) + snow * 0.06;\\n  let brightness = (0.008 + lit * grain)\\n    * fog * nearFade * wave * shimmer\\n    * (1.0 + pulse * (0.18 + pulses * 1.1))\\n    * (1.0 + ripple * 0.75 + shock * 3.0);\\n  var tint = mix(MINT * 0.75, MINT_PALE, clamp(crest + snow * 0.5, 0.0, 1.0)) * brightness;\\n\\n  let glint = step(0.993, rnd) * diffuse * (0.5 + 0.5 * sin(t * 2.0 + rnd * 47.0));\\n  tint += vec3f(0.85, 1.0, 0.93) * glint * fog * 0.55;\\n\\n  // the pointer stirs nearby dust\\n  let pointer = vec2f((params.pointer.x - 0.5) * 2.0, (0.5 - params.pointer.y) * 2.0);\\n  let toPointer = vec2f((u - pointer.x) * aspect, sy - pointer.y);\\n  let excite = smoothstep(0.55, 0.0, length(toPointer)) * params.misc.y;\\n  let push = normalize(toPointer + vec2f(0.0001, 0.0)) * excite * (0.006 + 0.010 * rnd);\\n  tint *= 1.0 + excite * 1.3;\\n\\n  // particles float around their anchor, more freely up close\\n  let floatAmp = (0.0015 + (1.0 - v) * 0.006) * (1.0 + excite * 2.5);\\n  let drift = vec2f(\\n    sin(t * (0.35 + rnd * 0.5) + rnd * 40.0),\\n    cos(t * (0.28 + rnd * 0.45) + rnd * 23.0) * 1.4,\\n  ) * floatAmp;\\n\\n  // the shockwave shoves particles outward as it passes\\n  let shockPush = vec2f(toCentre.x / max(distance, 0.02) / aspect, toCentre.y / max(distance, 0.02)) * shock * 0.045;\\n\\n  let sizePx = clamp(16.0 / z, 0.65, 1.9)\\n    * (0.75 + 0.5 * hash21(seed + 27.0))\\n    * (1.0 + glint * 0.25 + ripple * 0.20 + shock * 0.75);\\n  let quad = array<vec2f, 6>(\\n    vec2f(-1.0, -1.0), vec2f(1.0, -1.0), vec2f(-1.0, 1.0),\\n    vec2f(-1.0, 1.0), vec2f(1.0, -1.0), vec2f(1.0, 1.0),\\n  );\\n  let corners = quad;\\n  let offset = corners[corner];\\n\\n  var out: Particle;\\n  out.position = vec4f(\\n    vec2f(u, sy) + drift + push + shockPush + offset * sizePx * 2.0 / resolution,\\n    0.5,\\n    1.0,\\n  );\\n  out.local = offset;\\n  out.tint = tint * (1.0 - occluded) * clearing;\\n  return out;\\n}\\n\\n@fragment fn fs_main (@location(0) local: vec2f, @location(1) tint: vec3f) -> @location(0) vec4f {\\n  return vec4f(tint * smoothstep(1.0, 0.45, length(local)), 1.0);\\n}\\n\",o=1098240,s=2688,c=1.75,l=16,u=.22,d=.35,f=.55,p=48;function m(e,t,n,a){let o=1-Math.exp(-Math.min(Math.max(n,0),.05)/Math.max(a,.001));return[e[0]+(t[0]-e[0])*o,e[1]+(t[1]-e[1])*o]}function h(e,t,n){let r,i,h,v,w,g,x,y=!1,P=0,A=1,b=1,M=[.5,.5],E=[.1,.1],z=[.5,.5],_=0,k=!1,C=0,q=0,F=0,R=-1/0,L=!1,O=!1,N=!1,S=0,T=0,B=window.matchMedia(\"(prefers-reduced-motion: reduce)\").matches,X=new Float32Array(p/4),H=()=>{if(!r||!i)return;let n=e.getBoundingClientRect();if(n.width<=0||n.height<=0)return;let a=Math.min(window.devicePixelRatio||1,c,s/n.width),o=[Math.max(1,Math.floor(n.width*a)),Math.max(1,Math.floor(n.height*a))];(o[0]!==A||o[1]!==b)&&(A=o[0],b=o[1],e.width=A,e.height=b);let l=t.getBoundingClientRect();M=[(l.left+l.width/2-n.left)/Math.max(1,n.width),(l.top+l.height/2-n.top)/Math.max(1,n.height)],E=[Math.max(.001,l.width/Math.max(1,n.width)),Math.max(.001,l.height/Math.max(1,n.height))]},Z=t=>{if(\"touch\"===t.pointerType)return;let n=e.getBoundingClientRect();x=[Math.min(1,Math.max(0,(t.clientX-n.left)/Math.max(1,n.width))),Math.min(1,Math.max(0,(t.clientY-n.top)/Math.max(1,n.height)))]},I=()=>{x=void 0,k=!1},U=()=>{k=!0},W=()=>{k=!1},D=e=>{if(e!==O&&!y){if(e&&!L)return void(N=!0);if(O=e,O)return S=performance.now(),x=void 0,k=!1,P&&cancelAnimationFrame(P),void(P=0);N=!1,T+=performance.now()-S,R=-1/0,F=(performance.now()-T)/1e3,P||=requestAnimationFrame(G)}},G=e=>{if(y||(P=requestAnimationFrame(G),e-R<l)||!r||!i||!h||!v||!w)return;R=e;let t=(e-T)/1e3,n=Math.min(Math.max(t-F,0),.05);F=t,z=m(z,x??z,n,u),_+=(+!!x-_)*(1-Math.exp(-n/d)),k?(q=1,C=(C+n*f)%1):q>0&&(C+=n*f,C>=1&&(C=0,q=0)),X.set([M[0],M[1],E[0],E[1],A,b,z[0],z[1],C,q,t,_]),r.queue.writeBuffer(v,0,X);let a=r.createCommandEncoder(),s=a.beginRenderPass({colorAttachments:[{view:i.getCurrentTexture().createView(),loadOp:\"clear\",storeOp:\"store\",clearValue:{r:0,g:0,b:0,a:1}}]});s.setPipeline(h),s.setBindGroup(0,w),s.draw(o),s.end(),r.queue.submit([a.finish()]),L||(L=!0,Y.onFirstFrame?.(),N&&D(!0)),B&&(cancelAnimationFrame(P),P=0)},K=()=>{y||(y=!0,P&&cancelAnimationFrame(P),P=0,g?.disconnect(),window.removeEventListener(\"pointermove\",Z),document.documentElement.removeEventListener(\"pointerleave\",I),n?.removeEventListener(\"pointerenter\",U),n?.removeEventListener(\"pointerleave\",W),v?.destroy(),r?.destroy(),r=void 0,i=void 0)},Y={ready:(async()=>{let o=await navigator.gpu.requestAdapter({powerPreference:\"low-power\"});if(!o)throw Error(\"No WebGPU adapter.\");let s=await o.requestDevice();if(y)return void s.destroy();r=s,r.lost.then(()=>K());let l=e.getContext(\"webgpu\");if(!l)throw Error(\"No WebGPU canvas context.\");i=l;let c=navigator.gpu.getPreferredCanvasFormat();i.configure({device:r,format:c,alphaMode:\"opaque\"});let d=r.createShaderModule({label:\"nuxt-loading-mountains\",code:a});h=r.createRenderPipeline({label:\"nuxt-loading-mountains\",layout:\"auto\",vertex:{module:d,entryPoint:\"vs_main\"},fragment:{module:d,entryPoint:\"fs_main\",targets:[{format:c,blend:{color:{srcFactor:\"one\",dstFactor:\"one\"},alpha:{srcFactor:\"one\",dstFactor:\"one\"}}}]}}),v=r.createBuffer({label:\"nuxt-loading-params\",size:p,usage:GPUBufferUsage.UNIFORM|GPUBufferUsage.COPY_DST}),w=r.createBindGroup({layout:h.getBindGroupLayout(0),entries:[{binding:0,resource:{buffer:v}}]}),H(),g=typeof ResizeObserver>\"u\"?void 0:new ResizeObserver(H),g?.observe(e),g?.observe(t),window.addEventListener(\"pointermove\",Z),document.documentElement.addEventListener(\"pointerleave\",I),n?.addEventListener(\"pointerenter\",U),n?.addEventListener(\"pointerleave\",W),P=requestAnimationFrame(G)})().catch(e=>{throw K(),e}),dispose:K,setPaused:D};return Y}i();var g=document.querySelector(\"#nuxt-shader\"),_=document.querySelector(\"#nuxt-logo-slot\"),v=g&&_&&navigator.gpu?h(g,_,document.querySelector(\".nuxt-lockup\")):void g?.remove();g&&v&&(v.onFirstFrame=()=>g.classList.add(\"is-ready\"),v.ready.catch(()=>g.remove()),window.addEventListener(\"pagehide\",()=>v.dispose(),{once:!0}));var y=e=>{document.documentElement.toggleAttribute(\"data-idle\",e),v?.setPaused(e)};window.addEventListener(\"blur\",()=>y(!0)),window.addEventListener(\"focus\",()=>y(!1)),document.addEventListener(\"visibilitychange\",()=>{document.hidden?y(!0):document.hasFocus()&&y(!1)}),(document.hidden||!document.hasFocus())&&y(!0);</script></head><body><canvas id=\"nuxt-shader\" aria-hidden=\"true\"></canvas><a href=\"https://nuxt.com/?utm_source=nuxt-loading-screen\" target=\"_blank\" rel=\"noopener\" class=\"nuxt-lockup\" id=\"nuxtImg\" aria-label=\"" + escapeHtml(messages.appName) + "\"> <span id=\"nuxt-logo-slot\" class=\"nuxt-logo-slot\"><svg xmlns=\"http://www.w3.org/2000/svg\" fill=\"none\" aria-hidden=\"true\" viewBox=\"0 0 48 32\"><path d=\"M26.88 32h17.76c.567 0 1.11-.2 1.6-.48s.997-.633 1.28-1.12c.283-.486.48-1.038.48-1.6s-.196-1.114-.48-1.6l-12-20.64a2.9 2.9 0 0 0-1.12-1.12c-.49-.28-1.193-.48-1.76-.48s-1.11.2-1.6.48-.837.634-1.12 1.12l-3.04 5.28L20.8 1.6c-.284-.487-.63-1-1.12-1.28S18.647 0 18.08 0s-1.11.04-1.6.32-.997.793-1.28 1.28L.32 27.2c-.284.486-.32 1.038-.32 1.6 0 .561.037 1.113.32 1.6.283.486.79.84 1.28 1.12s1.033.48 1.6.48h11.2c4.438 0 7.668-1.99 9.92-5.76l5.44-9.44 2.88-4.96 8.8 15.04H29.76zm-12.64-5.12H6.4L18.08 6.72 24 16.8l-3.921 6.836c-1.496 2.446-3.201 3.244-5.839 3.244\"/></svg> </span> <svg xmlns=\"http://www.w3.org/2000/svg\" fill=\"none\" aria-hidden=\"true\" class=\"nuxt-wordmark\" viewBox=\"56 8 72 24\"><path fill=\"currentColor\" d=\"M60.32 32c.346 0 .64-.287.64-.64V16.48s.8 1.44 2.24 3.84l6.24 10.72c.285.598.919.96 1.44.96h4.32V8h-4.32c-.288 0-.64.24-.64.64v15.04l-2.88-5.12L61.6 8.8c-.28-.497-.883-.8-1.44-.8H56v24zm55.84-17.28h2.08c.53 0 .96-.43.96-.96V9.6h4.48v5.12H128v3.84h-4.32v6.88c0 1.68.809 2.4 2.24 2.4H128V32h-2.72c-3.688 0-6.08-2.389-6.08-6.4v-7.04h-3.04zm-21.6 0v9.92c0 2.24-.841 4.13-2.08 5.44S89.53 32 87.2 32s-4.201-.61-5.44-1.92c-1.218-1.31-2.08-3.2-2.08-5.44v-9.92h2.56c.546 0 1.083.1 1.44.48.357.36.48.57.48 1.12v8.32c0 1.29.073 2.086.64 2.72.567.613 1.266.8 2.4.8 1.155 0 1.673-.187 2.24-.8.567-.634.64-1.43.64-2.72v-8.32c0-.55.123-.9.48-1.28.314-.334.644-.326 1.12-.32zm13.6 8.32 5.44-8.32h-4.16c-.524 0-.99.188-1.28.64l-2.56 3.84-2.4-3.68c-.29-.452-.916-.8-1.44-.8h-4l5.44 8.16L97.28 32h4.16c.52 0 .989-.514 1.28-.96l2.88-4.32 3.04 4.48c.291.446.76.8 1.28.8h4.16z\"/></svg> </a><div class=\"nuxt-corners\"><a class=\"nuxt-home\" href=\"https://nuxt.com/?utm_source=nuxt-loading-screen\" target=\"_blank\" rel=\"noopener\">nuxt.com</a> <span class=\"nuxt-version\">5.0.0-0</span></div><div class=\"nuxt-progress\" id=\"nuxt-progress\"><p class=\"nuxt-progress-status\" id=\"nuxt-progress-status\" aria-live=\"polite\"></p><div class=\"nuxt-loader-bar\"><span></span></div></div><script>if(void 0===window.fetch)setTimeout(()=>window.location.reload(),200);else{let t=200;const e=async()=>{try{if(!(await window.fetch(window.location.href,{headers:{accept:\"text/html\"}}).then(t=>t.text())).includes(\"__NUXT_LOADING__\"))return window.location.reload()}catch{}setTimeout(e,t),t=Math.min(Math.round(1.5*t),1e3)};e()}</script></body></html>"
}