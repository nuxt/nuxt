import { defineNitroPlugin } from '#internal/nitro'
// import { defu } from 'defu'
// import inlineRouteRules from '#nuxt-route-rules'

export default defineNitroPlugin(() => {
  // TODO: handle updating route rules at runtime
  // const config = useRuntimeConfig()
  // config.nitro.routeRules = defu(inlineRouteRules, config.nitro.routeRules)
})
