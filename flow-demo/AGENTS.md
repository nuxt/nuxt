This is a Nuxt project.

- Run the dev server in background with `pnpm run dev 2>&1`
- Verify features you add with agent-browser
- ALWAYS load the `check-nuxt-errors` skill after every interaction to ensure no Nuxt errors are present before proceeding.

