{
  "private": true,
  "name": "fixture-basic-types",
  "scripts": {
    "build": "nuxi build",
    "test:types": "nuxi prepare && npx vue-tsc --noEmit"
  },
  "dependencies": {
    "nuxt": "workspace:*"
  },
  "devDependencies": {
    "ofetch": "latest",
    "vitest": "1.5.2",
    "vue-router": "latest",
    "vue": "latest"
  }
}
