{
  "private": true,
  "name": "fixture-basic-types",
  "scripts": {
    "build": "nuxi build",
    "test:types": "nuxi prepare && npx vue-tsc --noEmit"
  },
  "dependencies": {
    "nuxt": "workspace:*"
  },
  "devDependencies": {
    "ofetch": "latest",
    "unplugin-vue-router": "^0.10.7",
    "vitest": "1.6.0",
    "vue": "latest",
    "vue-router": "latest"
  },
  "engines": {
    "node": ">=18.12.0"
  }
}
