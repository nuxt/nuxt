{
  "private": true,
  "name": "fixture-basic-types",
  "scripts": {
    "build": "nuxt build",
    "test:types": "nuxt prepare && npx vue-tsc -b --noEmit"
  },
  "dependencies": {
    "nuxt": "workspace:*"
  },
  "devDependencies": {
    "ofetch": "latest",
    "vitest": "latest",
    "vue": "latest",
    "vue-router": "5.0.3"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
