{
  "private": true,
  "name": "fixture-basic-types",
  "scripts": {
    "build": "nuxi build",
    "test:types": "nuxi prepare && npx vue-tsc --noEmit"
  },
  "dependencies": {
    "nuxt": "workspace:*"
  },
  "devDependencies": {
    "ofetch": "latest",
    "unplugin": "latest",
    "vitest": "latest",
    "vue-router": "latest"
  }
}
