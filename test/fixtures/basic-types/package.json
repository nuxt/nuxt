{
  "private": true,
  "name": "fixture-basic-types",
  "scripts": {
    "build": "nuxt build",
    "test:types": "nuxt prepare && npx vue-tsc -b --noEmit"
  },
  "dependencies": {
    "nuxt": "workspace:*",
    "ofetch": "^2.0.0-alpha.3",
    "vue": "^3.5.22",
    "vue-router": "^5.0.3"
  },
  "devDependencies": {
    "vitest": "4.0.18"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
