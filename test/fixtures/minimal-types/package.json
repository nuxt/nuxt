{
  "private": true,
  "type": "module",
  "name": "fixture-minimal-types",
  "scripts": {
    "build": "nuxt build",
    "test:types": "nuxt prepare && npx vue-tsc -b --noEmit"
  },
  "dependencies": {
    "nuxt": "workspace:*"
  },
  "engines": {
    "node": "^22.22.3 || ^24.15.0 || >=26.0.0"
  }
}
