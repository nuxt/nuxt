{
  "private": true,
  "name": "fixture-minimal-types",
  "scripts": {
    "build": "nuxt build",
    "test:types": "nuxt prepare && npx vue-tsc --noEmit"
  },
  "dependencies": {
    "nuxt": "workspace:*"
  },
  "engines": {
    "node": ">=22.12.0"
  }
}
