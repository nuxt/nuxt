{
  "private": true,
  "name": "fixture-minimal-types",
  "scripts": {
    "build": "nuxt build",
    "test:types": "nuxt prepare && npx vue-tsc -b --noEmit"
  },
  "dependencies": {
    "nuxt": "workspace:*"
  },
  "engines": {
    "node": "^22.12.0 || ^24.11.0 || >=26.0.0"
  }
}
