{
  "private": true,
  "name": "fixture-minimal-types",
  "scripts": {
    "build": "nuxi build",
    "test:types": "nuxi prepare && npx vue-tsc --noEmit"
  },
  "dependencies": {
    "nuxt": "workspace:*",
    "vue": "latest"
  }
}
