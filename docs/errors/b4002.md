---
title: 'NUXT_B4002'
description: 'await in definePageMeta.'
navigation: false
---

# B4002

The `await` keyword was used inside `definePageMeta()`. Page metadata is extracted at build time through static analysis, so it must be defined synchronously.

## Resolution

Move async logic out of `definePageMeta()` — use route middleware or the component's `setup()` instead.
