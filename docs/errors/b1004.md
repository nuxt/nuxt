---
title: 'NUXT_B1004'
description: 'Failed to install package.'
navigation: false
---

# B1004

Nuxt detected a missing dependency and tried to install it automatically, but the installation failed (e.g. network error, registry unavailable, or insufficient permissions).

## Resolution

Install the package manually with your package manager, for example:

```bash
npm install <package>
```
