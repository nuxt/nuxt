---
title: 'NUXT_B8017'
description: 'Could not load module'
navigation: false
---

# B8017

A module referenced in your config could not be resolved — the package is not installed or its name is misspelled.

## Resolution

Install the package and verify the name in `nuxt.config.ts` matches:

```bash
npm install <module-name>
```
