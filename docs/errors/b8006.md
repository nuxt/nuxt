---
title: 'NUXT_B8006'
description: 'Cannot find Nuxt from cwd'
navigation: false
---

# B8006

The `nuxt` package could not be resolved from the current working directory — either it is not installed, or the command was run outside a Nuxt project.

## Resolution

Install Nuxt and run commands from the project root:

```bash
npm install nuxt
```
