---
title: 'NUXT_B1003'
description: 'Invalid template.'
navigation: false
---

# B1003

A template was registered without the required `src` or `getContents` property. Every template must provide one of these so Nuxt knows how to generate its content.

## Resolution

Provide either `src` or `getContents` when calling `addTemplate()`. If a module registers the template, ensure it is up to date.
