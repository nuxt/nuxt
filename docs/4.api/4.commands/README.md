---
navigation: false
---

# Nuxt CLI Commands

The Nuxt CLI command reference now lives in the `docs/` directory of <https://github.com/nuxt/cli>, which is the source of truth for these pages.

Editing files in this directory has no effect on the website. Please open your pull request against `nuxt/cli` instead.
